import type { LandingTestimonial } from '@/components/landing/landing-testimonials';

/** Populate when real quotes are approved; empty array hides the section on the home page. */
export const marketingTestimonialItems: readonly LandingTestimonial[] = [];

import { test } from '@playwright/test';

test.describe('queue-display experience acceptance', () => {
  test('remains reserved until a deterministic assigned-unit fixture is available', async () => {
    test.fixme(
      true,
      'requires a seeded unit assignment, public manifest route, and stable viewport fixture before acceptance'
    );
  });
});
